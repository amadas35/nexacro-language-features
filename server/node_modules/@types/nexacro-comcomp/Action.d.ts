// Type definitions for Nexacro N
// Definitions: https://github.com/DefinitelyTyped/DefinitelyTyped
// TypeScript Version: 3.8;
    
interface NexacroAPI {
    Action: nexacro.Action;
}

    
declare namespace nexacro {
    type ActionOmitKeys = 'id';
	
	interface Action extends Omit<nexacro.EventSinkObject, ActionOmitKeys> {
	    /**
		 * @constructor
		 */
		new(): nexacro.Action;
	    /**
		 * This read-only property holds the parent object of a Action.
		 */
		readonly parent?: object;
		readonly targetview?: string;
		readonly onerror?: nexacro.EventObject<nexacro.Action, nexacro.ActionEventInfo>;
		readonly onsuccess?: nexacro.EventObject<nexacro.Action, nexacro.ActionEventInfo>;
		set_targetview(v: string): void;
		/**
		 * This method is used to delete a Action that is dynamically created through a script.
		 * @returns {boolean} 
		 */
		destroy(): boolean;
		getContents(strKey?: string): object;
		getTargetView(): object;
		run(): null;
	}
}

