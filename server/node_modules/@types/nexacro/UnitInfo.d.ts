// Type definitions for Nexacro N
// Definitions: https://github.com/DefinitelyTyped/DefinitelyTyped
// TypeScript Version: 4.3;
    
declare namespace nexacro.Unit {
    type Size = `${number}px`;
	type PixelPercent = `${number}${'px'|'%'}`;
	type PositionStep = void;
	type Opacity = void;
	type Point = `${number}px`;
	type PositionType = `${number}px`;
	type TransformRotate = `${number}deg`;
	type FontSize_Point = `${number}pt`;
	type Shadow = void;
	type ShadowOffset = `${number}px`;
	type Percent = `${number}%`;
	type Quality = void;
	type Pixel = `${number}px`;
	type FontSize2 = `${number}${'pt'|'px'}`;
	type PixelNegative = `${number}px`;
}
